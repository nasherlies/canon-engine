# CURSOR RULES — CANON ENGINE (pointer only)

**Use the repo-root `.cursorrules` file.** It is the only copy Cursor loads for agent law.

Do **not** edit **this** file to change project rules—edit **`.cursorrules`** instead.

This file existed as a redundant duplicate and now exists only so older bookmarks still land on a pointer.
